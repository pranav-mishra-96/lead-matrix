function App() {
  return (
    <div
      style={{
        fontFamily: "system-ui, sans-serif",
        maxWidth: "600px",
        margin: "4rem auto",
        padding: "0 1rem",
        lineHeight: 1.6,
      }}
    >
      <h1>Strategic Lead Matrix</h1>
      <p>Frontend container is running. Ready to build the chat interface.</p>
      <p style={{ color: "#666", fontSize: "0.9rem" }}>
        Status: container healthy · API proxy configured
      </p>
    </div>
  );
}

export default App;